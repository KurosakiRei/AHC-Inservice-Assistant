name: deploy

on:
  push:
    branches:
      - dev

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup node
        uses: actions/setup-node@v4
        with:
          node-version: "lts/*"
          cache: "npm"

      - run: npm ci
      - run: npm run build

      - name: Remove non-prod build artifacts
        run: rm -f dist/index.debug.js dist/index.dev.user.js

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: dist
          publish_dir: ./dist
          enable_jekyll: false
          commit_message: deploy ${{ github.ref }}
          user_name: github-actions[bot]
          user_email: github-actions[bot]@users.noreply.github.com
